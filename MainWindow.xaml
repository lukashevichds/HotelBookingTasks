<Window x:Class="HotelBookingTasks.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Hotel Booking Tasks"
        Height="720"
        Width="1280"
        MinHeight="600"
        MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="{DynamicResource PrimaryBrush}">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="26"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="Hotel Booking Tasks"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="White"
                               TextWrapping="Wrap"/>

                    <TextBlock Text="Клиентская система сопровождения бронирований"
                               Margin="0,8,0,0"
                               Foreground="White"
                               Opacity="0.9"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <Border Grid.Row="1"
                        Background="#26FFFFFF"
                        CornerRadius="10"
                        Padding="12"
                        Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Текущий пользователь"
                                   Foreground="White"
                                   Opacity="0.82"/>
                        <TextBlock x:Name="CurrentUserTextBlock"
                                   Margin="0,4,0,0"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="White"/>
                        <TextBlock x:Name="CurrentRoleTextBlock"
                                   Margin="0,2,0,0"
                                   Foreground="White"
                                   Opacity="0.88"/>
                    </StackPanel>
                </Border>

                <Button Grid.Row="3"
                        Content="Бронирования"
                        Click="BookingsButton_Click"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,0"/>

                <Button Grid.Row="5"
                        Content="Задачи"
                        Click="TasksButton_Click"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,0"/>

                <Button x:Name="MonitoringButton"
                        Grid.Row="9"
                        Content="Мониторинг"
                        Click="MonitoringButton_Click"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,0"/>

                <Button Grid.Row="7"
                        Content="Чат-боты"
                        Click="ChatBotsButton_Click"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,0"/>

                <StackPanel Grid.Row="11">
                    <TextBlock Text="Полный сценарий для защиты: admin / admin"
                               Foreground="White"
                               Opacity="0.85"
                               TextWrapping="Wrap"
                               Margin="0,0,0,10"/>

                    <Button Content="Выход"
                            Click="ExitButton_Click"
                            HorizontalAlignment="Stretch"
                            Background="White"
                            Foreground="{DynamicResource TextBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1"
              Margin="20">
            <Border Style="{StaticResource CardBorderStyle}"
                    Padding="0"
                    Margin="0">
                <Frame x:Name="MainFrame"
                       NavigationUIVisibility="Hidden"
                       Background="White"/>
            </Border>
        </Grid>
    </Grid>
</Window>
